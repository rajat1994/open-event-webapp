<div class="pad--large underline">
  <h2 class="ms1 text-center">Our Speakers</h2>
</div>

<div class="pad m-top">
  <div class="flex flex--row speakers">
    {{#each model as |speaker|}}
      {{speaker-summary data=speaker}}
    {{/each}}
  </div>
</div>
